
This is a simple element zapper tool operated by hotkey. Load it into Google extensions via developer mode and make sure the hotkey is set, and get zapping. Useful for annoying little elements that slip through adblockers. Default hotkey is CMD + CTRL + n


